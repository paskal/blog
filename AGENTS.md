# Codex Instructions

Keep `CLAUDE.md` as the canonical project memory and read it before substantial work. Repository skills are canonical in `.claude/skills`. In `.agents/skills`, `google-search-console` and `plausible` link to their project-owned canonical skills; the shared `seo-*` entries link to Codex adapters in the sibling `favor-group.ru` checkout, and those adapters load this repository's canonical SEO skills.

The shared SEO suite is generic. Do not load favor-group project adapters, credentials, business rules or client data while working on this blog.
