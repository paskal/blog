{{ "<?" | safeHTML }}xml-stylesheet type="text/xsl" href="{{ "/sitemap.xsl" | absURL }}"{{ "?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {{ range .Data.Pages }}
    {{/* Listing pages are noindex in head.html, so keep them out of the sitemap
         too. Matching on kind also covers the /ru/ mirrors, which a RelPermalink
         prefix check misses. */}}
    {{ if not (in (slice "section" "taxonomy" "term") .Kind) }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ safeHTML ( .Lastmod.Format "2006-01-02T15:04:05-07:00" ) }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>{{ end }}
  </url>
  {{ end }}
    {{ end }}
</urlset>
